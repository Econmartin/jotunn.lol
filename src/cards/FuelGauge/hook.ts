/**
 * @hook useFuelGauge
 * @status IMPLEMENTED
 * Finds the Network Node in owned objects, fetches its dynamic fields,
 * and extracts the numeric fuel value.
 */
export { useOwnedObjects } from "../../hooks/useOwnedObjects";
export { useObjectWithDynamicFields } from "../../hooks/useObjectWithDynamicFields";
