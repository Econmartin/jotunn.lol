/**
 * @hook useTxHistory
 * @status IMPLEMENTED
 * Fetches wallet transactions via Sui GraphQL (paginated, affectedAddress filter).
 */
export { useTransactions } from "../../hooks/useTransactions";
